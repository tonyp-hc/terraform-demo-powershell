variable "TriggerValue" {
  default = "1"
  description = "Some value that, when changed, should trigger the provisioner to run. For example, a change in resource allocation that triggers some budget allocation workflow" 
}
